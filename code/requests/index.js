let buildRequests = require('./buildRequests.js');
let makeRequests = require('./makeRequests.js');

module.exports = {
  buildRequests: buildRequests.buildRequests,
  makeRequests: makeRequests.makeRequests,
}
